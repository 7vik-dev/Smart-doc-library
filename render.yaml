services:
  - type: web
    name: smart-doc-library
    env: docker
    dockerContext: .
    dockerfilePath: Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: smart-doc-db
          property: connectionString

databases:
  - name: smart-doc-db
    databaseName: smart_doc_library
    user: smart_doc_user
    plan: free
    region: oregon
